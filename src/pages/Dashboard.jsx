import Layout from "../components/Layout";

export default function Dashboard() {
  return (
    <Layout>
      {/* No content needed here — Layout handles the welcome block */}
    </Layout>
  );
}